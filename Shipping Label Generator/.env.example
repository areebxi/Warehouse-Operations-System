# Shipping Label Generator — optional app overrides (not ShipStation API keys).
# API credentials live only in config/ShipStation/.env

SHIPPING_PROVIDER=real

# Optional (leave blank to use shipping_config.yaml defaults)
MAX_CONCURRENCY=
SHIP_FROM=
PROCESSED_BY=
