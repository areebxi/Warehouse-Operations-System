# runtime/

Shared cross-app I/O only:

- `SharedInbox/DTF Des/{date}/{shift}/` — Packing dual-write → Queue Missing Logo watcher

Per-app Input/Output/Logs live inside each app folder.
Resolve via `shared/paths.py`.
