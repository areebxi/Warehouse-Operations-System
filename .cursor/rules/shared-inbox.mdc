---
description: SharedInbox DTF Des ownership — packing writes, Queue consumes
alwaysApply: false
globs: "runtime/SharedInbox/**"
---

# SharedInbox

- Packing dual-writes `DTF Des-P*.xlsx` into `runtime/SharedInbox/DTF Des/{date}/{shift}/`.
- Queue Missing Logo auto-watcher processes those files (no approval) and moves them to `Processed/` or `Failed/`.
- Path helper: `shared.paths.shared_inbox_dtf_des_root()`.
- Do not delete live inbox files while packing or the watcher may be writing.
- Shipping does not auto-read this folder yet.
